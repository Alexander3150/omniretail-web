"use client";

import Link from "next/link";
import { OrderStatus } from "@/core/enums";
import { useStorefrontOrderTracking } from "@/modules/storefront/hooks/useStorefrontOrderTracking";

const statusLabels: Record<OrderStatus, string> = { [OrderStatus.pending]: "Confirmado", [OrderStatus.confirmed]: "Confirmado", [OrderStatus.preparing]: "En preparación", [OrderStatus.picking]: "En preparación", [OrderStatus.packing]: "En preparación", [OrderStatus.ready_for_pickup]: "En preparación", [OrderStatus.ready_for_dispatch]: "En preparación", [OrderStatus.dispatched]: "Enviado", [OrderStatus.delivered]: "Enviado", [OrderStatus.cancelled]: "Cancelado" };
const flow = ["Confirmado", "En preparación", "Enviado"];
function stageFor(status: OrderStatus) { if (status === OrderStatus.dispatched || status === OrderStatus.delivered) return 2; if (status === OrderStatus.pending || status === OrderStatus.confirmed) return 0; return 1; }
export function OrderTrackingPage({ trackingToken }: { trackingToken: string }) { const { data, loading, error, reload } = useStorefrontOrderTracking(trackingToken); if (loading) return <main className="mx-auto max-w-4xl px-5 py-12 text-[var(--color-text-muted)]">Cargando pedido...</main>; if (error || !data) return <main className="mx-auto max-w-4xl px-5 py-12"><h1 className="text-3xl font-black">Seguimiento de pedido</h1><p className="mt-4 text-[var(--color-text-muted)]">{error ?? "No se encontró el pedido."}</p><div className="mt-5 flex gap-3"><button className="rounded-xl bg-[var(--color-primary)] px-4 py-3 font-bold text-[var(--color-topbar)]" onClick={reload} type="button">Reintentar</button><Link className="rounded-xl border border-[var(--color-border)] px-4 py-3 font-bold" href="/catalogo">Ver catálogo</Link></div></main>; const activeIndex = stageFor(data.status); const cancelled = data.status === OrderStatus.cancelled; return <main className="mx-auto max-w-4xl px-5 py-10"><Link className="text-sm font-bold text-[var(--color-title)]" href="/catalogo">← Seguir comprando</Link><section className="mt-5 overflow-hidden rounded-3xl border border-[var(--color-border)] bg-[var(--color-surface)] shadow-sm"><div className="bg-[var(--color-topbar)] px-6 py-7 text-white"><p className="text-sm text-slate-300">Pedido {data.orderNumber}</p><h1 className="mt-2 text-3xl font-black">Seguimiento de pedido</h1><p className="mt-3 inline-block rounded-full bg-[var(--color-primary)]/20 px-3 py-1 text-sm font-bold text-[var(--color-primary)]">{statusLabels[data.status]}</p></div><div className="p-6">{!cancelled ? <ol className="grid gap-3 sm:grid-cols-3">{flow.map((label, index) => <li key={label} className="flex items-center gap-2 sm:block"><span className={`grid h-9 w-9 place-items-center rounded-full text-sm font-black ${index <= activeIndex ? "bg-[var(--color-primary)] text-[var(--color-topbar)]" : "bg-slate-200 text-slate-500"}`}>{index + 1}</span><p className={`text-sm font-bold sm:mt-2 ${index <= activeIndex ? "text-[var(--color-text)]" : "text-[var(--color-text-muted)]"}`}>{label}</p></li>)}</ol> : <p className="rounded-xl bg-red-50 p-4 text-sm font-bold text-[var(--color-danger)]">Este pedido fue cancelado.</p>}<div className="mt-8 space-y-3">{data.items.map((item, index) => <article key={`${item.sku}-${index}`} className="flex items-center justify-between gap-4 rounded-xl bg-slate-50 p-4"><div><p className="text-xs font-bold uppercase tracking-wider text-[var(--color-primary-hover)]">{item.sku}</p><h2 className="mt-1 font-black text-[var(--color-text)]">{item.name}</h2><p className="mt-1 text-sm text-[var(--color-text-muted)]">Cantidad · {item.quantity}</p></div><p className="font-black text-[var(--color-text)]">Q{item.subtotal.toFixed(2)}</p></article>)}</div><div className="mt-6 flex items-center justify-between border-t border-[var(--color-border)] pt-5"><span className="font-bold text-[var(--color-text)]">Total</span><span className="text-3xl font-black text-[var(--color-title)]">Q{data.total.toFixed(2)}</span></div></div></section></main>; }
