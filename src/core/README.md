# core

`core/` contiene los contratos oficiales compartidos por todo OmniRetail.

Los modulos no deben crear versiones duplicadas de entidades como `Product`, `Order`, `Customer`, `Supplier`, `User` o `Sale`. Esos contratos se definiran en una fase posterior.

En esta fase solo se prepara la estructura base.
