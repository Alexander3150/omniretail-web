# infrastructure

`infrastructure/` contendra la implementacion simulada del backend: MockDatabase, repositories mock, storage, eventos internos y providers.

No se implementan repositories, providers ni base mock definitiva en esta fase. Esos elementos se agregaran cuando `core/` tenga contratos oficiales.
